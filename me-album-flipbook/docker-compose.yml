version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: album-flipbook_server_1
      APP_PORT: 8080

  server:
    image: ${APP_FLIPBOOK_IMAGE}
    restart: on-failure
    stop_grace_period: 1m
    volumes:
      - ${APP_DATA_DIR}/data:/data
